common --registry="https://raw.githubusercontent.com/dejwk/roo-registry/main/" --registry="https://bcr.bazel.build"

import %workspace%/bazelrc/esp32/base.bazelrc
import %workspace%/bazelrc/esp32/idf.bazelrc
import %workspace%/bazelrc/esp32/arduino.bazelrc

# Preserve the historical roo_testing smoke command (`bazel test :all`) by
# selecting Arduino in this repository only. Client roots select one frontend
# explicitly; IDF checks use a clean rc context so the profiles never combine.
build --config=roo_testing_arduino_esp32

build:asan --strip=never
build:asan --copt -fsanitize=address
build:asan --copt -DADDRESS_SANITIZER
build:asan --copt -O1
build:asan --copt -g
build:asan --copt -fno-omit-frame-pointer
build:asan --linkopt -fsanitize=address
# GCC 14 can emit spurious maybe-uninitialized warnings in std::regex with ASAN.
build:asan --per_file_copt=.*glog\+.*logging\.cc@-Wno-maybe-uninitialized
